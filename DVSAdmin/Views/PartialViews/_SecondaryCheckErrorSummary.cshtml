@model DVSAdmin.Models.SecondaryCheckViewModel;
<div class="govuk-error-summary" data-module="govuk-error-summary">
    <div role="alert">
        <h2 class="govuk-error-summary__title">
            There is a problem
        </h2>
        <div class="govuk-error-summary__body">
            <ul class="govuk-list govuk-error-summary__list">

                @if (ViewData.ModelState["SubmitValidation"] !=null)
                {
                    @foreach (var error in ViewData.ModelState["SubmitValidation"].Errors)
                    {
                        <li>
                            <a href="#submit-error">@error.ErrorMessage</a>
                        </li>
                    }
                }
                @if (ViewData.ModelState["Comment"] !=null)
                {
                    @foreach (var error in ViewData.ModelState["Comment"].Errors)
                    {
                        <li>
                            <a href="#comment-error">@error.ErrorMessage</a>
                        </li>
                    }
                }
            </ul>
        </div>
    </div>
</div>